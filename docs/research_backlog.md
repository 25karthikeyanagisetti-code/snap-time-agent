# Research backlog

The daily scheduled task pops the top item off this list, implements it, runs it, and commits the result. If this list is empty, the daily task generates a fresh hypothesis from the most recent findings.

## Format

Each item is a single bullet with: `<short_name>` — one-sentence hypothesis — what to vary — what to measure.

---

## Queued

- `impact_decomp_n200_threshold` — N=200 replication at β_loyalty ∈ {0.30, 0.40} (the threshold-straddling cells from impact_decomp_beta_sweep). Definitively settles whether the 0.05 impact-gap threshold is behaviorally meaningful or inert. If penalty is inside ±4 pts, source-vs-gate isomorphism is complete. (New 2026-06-04 from impact_decomp_beta_sweep PARTIAL.)
- `decision_step_trace` — Log the step at which top-1 recall fires per episode at β_loyalty ∈ {0.30, 0.50}. Test whether seed_refresh_capped shifts the modal firing step earlier (over-steering) vs seed_only_floor. Finer-grained metric than binary rescue rate; can confirm the impact-timing story at N=40. (New 2026-06-04 from impact_decomp_beta_sweep PARTIAL.)
- `impact_vs_injection_dissociation` — Run seed_refresh_capped with injection suppressed (deliver impact boost without emotion bleed-in) to isolate whether the 7.5-pt rescue penalty at β_loyalty=0.50 is injection-mediated or impact-mediated. If rescue recovers to floor's 80%, the hurt comes through the guilt bleed-in; if rescue stays depressed, impact dominance itself (seed monopolizing top-1 slot) is the mechanism. (New 2026-05-29 from capped_floor_impact_decomp.)
- `off_baseline_stability_audit` — Re-run the OFF arm alone at all 4 β_loyalty cells with N=200 to map the OFF distribution. Today's experiment showed the β=0.05 OFF drifted 17 pts (62.5% → 79.5%) between N=40 runs, large enough to fake "consistent direction" endpoint effects. If OFF varies >5 pts across cells at N=200, future PARTIAL classifications need an OFF drift flag before mechanism interpretation. (New 2026-05-21 from capped_loyalty_n200_endpoints HELD.)
- `floor_on_gate_with_source_max_inject` — Third mode that applies max(stored, floor) at injection time but reads stored.emotion from a derived view (no overwrite of the memory). Cleanest falsifier: if it matches seed_only_floor exactly at all four loyalty cells, the endpoint divergence in capped IS the impact-term amplification on the source; if it matches seed_refresh_capped, the mechanism lives post-recall. (New 2026-05-20.)
- `capped_floor_outcome_attach` — Attach per-memory `floor` to FAILURE-tagged and RESCUE-tagged outcome memories at encoding time; drop the corresponding tag entries from TAG_FLOORS_DEFAULT. Tests whether the source-application strategy that compressed the seed dispatch extends from a once-created memory (the seed) to repeatedly-created memories (outcomes). If yes, the tag dispatch table is fully eliminable. (New 2026-05-19 from seed_refresh_capped HELD.)
- `capped_floor_n200` — Replicate seed_refresh_capped's β_guilt=0.05 cell (the largest residual gap, 7.5 pts) at N=200 to tighten the substitutability claim from "inside 2-SE band" to "≤3 pts." Sharper test of whether the source-vs-gate isomorphism is exact or carries a small systematic residual at low β_guilt. (New 2026-05-19.)
- `capped_floor_long_chain` — Run seed_refresh_capped vs seed_only_floor at chain_length=30 to test whether the equivalence survives past the chain horizon where outcome-encoded memories dominate the store and the seed memory's relative weight degrades. ep5–9 today shows the two modes within 4.5 pts but at chain=10 the store is still seed-heavy. (New 2026-05-19.)
- `seed_refresh_b50_n200` — Replicate β_guilt=0.50 at N=200 to tighten the 2.5-pt seed_refresh-vs-seed_only_floor substitutability claim. The OFF baseline this run (40.0%) is 17.5 pts below the 2026-05-12 N=40 reading (57.5%) — either intrinsic N=40 noise at this cell or a seed-pattern effect. (New 2026-05-18.)
- `seed_refresh_kappa_sweep` — Replicate exp_seed_refresh at κ ∈ {0.5, 2.0}. Tests whether the over-correction story persists under different decision-regime gains. Predicts: penalty grows at κ=2.0 (saturated-committed locks deeper); penalty shrinks at κ=0.5 (boomerang shoulder has compressed dynamic range). (New 2026-05-18.)
- `seed_only_floor_b30_long_chain` — Replicate β_guilt=0.30 at chain_length=20+ to clarify whether the 4.8-pt ep5–7 full-vs-seed gap observed in seed_only_floor_b30_n200 (within noise at N=200, but suggestive) is a real long-run outcome-floor contribution or chain-aging noise. Pairs with seed_only_floor_ep5_9_audit's existing β_guilt=0.50 plan. (New 2026-05-13.)
- `seed_only_floor_ep5_9_audit` — Replicate seed_only_floor's β_guilt=0.50 cell at chain_length=30. Tests whether the +9 pts ep5–9 uplift is a finite-window artifact or persists. If sustained past ep10, the long-run capacity at the regime-break cell IS seed-prior-driven. (New 2026-05-12.)
- `floor_negative_control` — Replace the seed floor with a random emotion template of matched magnitude (but different vector direction). If the effect persists, seed-template fidelity doesn't matter; any high-magnitude guilt-charged floor would do. (New 2026-05-12.)
- `recall_event_trace` — During a κ=1.0 chain, log every memory reactivation event (which memory fired, at what step, into what context, top-1 impact). Hypothesis: at high β_guilt, the top-1 recall slot during early ep0 deliberation drifts from the seeded prior to a partner-adjacent rescue memory, routing injection through a loyalty-floor template. Promoted 2026-05-11. (Originally elevated 2026-05-04 after loyalty_importance_floor null.)
- `tag_injection_beta50_n200` — Replicate the β_guilt=0.50 cell with N=200 agents to tighten the −2.0 pts ep0 delta from the 2026-05-11 run to ±5 pts. (New 2026-05-11.)
- `tag_aware_recall_kappa` — Replicate exp_tag_aware_recall at κ ∈ {0.5, 2.0}. Tests whether the ep5–9 uplift is regime-coupled or generic. Predicts: smaller uplift at κ=0.5 (boomerang regime can't sustain rescue at all) and bounded uplift at κ=2.0 (already saturated-committed). (Elevated 2026-05-09.)
- `laundering_inflection` — Fine sweep β_guilt ∈ {0.05, 0.07, 0.09, 0.11, 0.13, 0.15} at κ=1.0 to find the exact threshold where failure memories switch from class-preserving to class-flipping. (Elevated 2026-05-07.) Now sharper: can be run in tag-aware mode to isolate the injection-side mechanism.
- `recharge_on_recall` — Add `+= δ` injection to stored emotion every time a memory reactivates above threshold. If this prevents laundering AND preserves divergence, we have an alternative counter-mechanism that operates on stored state instead of class identity. (Elevated 2026-05-07; tag_aware_recall now provides a tag-keyed comparison baseline.)
- `decay_asymmetry_lower_kappa` — Replicate the 2026-05-05 4-cell sweep at κ=0.5 (boomerang is weaker there). Tests whether the divergence-inversion is regime-specific to deep-committed κ=1.0 or generic across the boomerang regime.
- `decay_asymmetry_fine_reversed` — Resolve whether the divergence falloff under β_guilt is monotone or has a small positive bump at low-but-asymmetric β_guilt. Sweep β_guilt ∈ {0.05, 0.10, 0.15, 0.20, 0.25, 0.30} with β_loyalty=0.05 at κ=1.0. Measure: divergence@5–9 fine-grained.
- `regime_break_check` — Re-run the 2026-05-05 loyalty-side sweep with β_loyalty extended to {0.70, 0.90} to test whether ep0 also collapses on the loyalty side at sufficiently extreme values, or whether the regime-break is unique to the guilt-side. Distinguishes "extreme asymmetry breaks regime" from "extreme guilt-side decay strips seeded prior."
- `seed_charge_match` — Scale the seeded abandonment-prior guilt charge to match the encoded rescue memory's loyalty magnitude. Re-run the reversed-asymmetry sweep. If divergence becomes signed, the cushion-vs-counterweight story partially survives — only obscured by a charge-magnitude asymmetry between seed and encoding. Falsifier of "magnitude-asymmetry alone explains the prior result."
- `rescue_payload_magnitude` — The boomerang is driven by the rescue memory's loyalty=0.8 emotion magnitude entering recall via the γ-term, not its importance. Vary: rescue payload loyalty ∈ {0.0, 0.2, 0.4, 0.6, 0.8}. Measure: ep5–9 rescue rate at κ=1.0. (Follow-up to loyalty_importance_floor null.)
- `recall_event_trace` — During a κ=1.0 chain, log every memory reactivation event (which memory fired, at what step, into what context). Hypothesis: rescue memories reactivate during deliberation steps (not only on rescue-cell terminal contexts), pulling emotion off the committed trajectory. (Follow-up to loyalty_importance_floor null.)
- `prior_dilution_rate` — At high encoding-gate τ, the seeded abandonment prior dominates indefinitely. Vary: prior preage ∈ {0, 5, 15, 30, 60}. Measure: rescue@ep9 at τ=0.7. (Follow-up to selective_encoding null.)
- `memory_consolidation` — Periodic pruning of low-impact memories preserves the committed-rescuer regime. Vary: prune_interval ∈ {1, 3, 5, 10}, prune_threshold ∈ {0.05, 0.15, 0.3}. Measure: rescue rate at episode 9 vs episode 0 across κ.
- `dynamic_kappa` — Annealing κ from high to low across episodes prevents paralysis early then allows rationality late. Vary: schedule ∈ {constant, linear_decay, step_decay}. Measure: failure rate trajectory across 10 episodes.
- `multi_memory` — Two seeded memories (loyalty AND guilt) cancel out — agent reverts to value-greedy. Vary: number of seeded memories ∈ {0, 1, 2, 3}. Measure: failure rate at κ=0.25.
- `partner_deadline_sweep` — The Paralysis Valley shifts with the partner deadline. Vary: deadline ∈ {3, 5, 7, 10, 15}. Measure: κ_min of the failure peak.
- `jitter_sigma_sweep` — Find the sweet spot for the Encoding Diversity Effect. Vary: encoding_jitter σ ∈ {0.05, 0.10, 0.15, 0.25, 0.50, 0.80}. Measure: sustained rescue rate ep5-9 at κ=1.0. (Follow-up to 2026-05-02 personality_emergence headline.)
- `jitter_kappa_sweep` — Does encoding diversity rescue the population at lower κ regimes too, or only at κ=1.0? Vary: κ ∈ {0.1, 0.25, 0.5, 1.0, 2.0, 4.0} with jitter σ=0.15 fixed. Measure: sustained rescue rate ep5-9. Predicts: largest gain at κ=1.0, vanishing gain near κ=0.
- `jitter_chain_length` — Sustained-capacity claim must hold past 10 episodes. Run jitter ON vs OFF at chain_length=30. Measure: rescue rate at ep20 and ep30. If jitter ON stays at ~39% but baseline crashes to <5%, the effect is structural; if both crash, it's a finite-window artifact.
- `jitter_plus_tag_aware` — The jitter mechanism (population spread) and the tag-aware recall mechanism (valence-laundering fix) are independent. Combine them. Measure: divergence@5-9 in jitter+tag-aware vs jitter alone. If divergence finally goes positive (>+15 pts) we have FULL behavioral types.
- `noise_typology` — Different noise types (Gaussian on emotion vs uniform on Φ vs softmax temperature) have qualitatively different rescue effects. Vary: noise_target ∈ {emotion, phi, temperature}. Measure: failure rate at κ=0.5.
- `agent_memory_seed` — Letting the agent encode its OWN early-episode memory before the seeded one shifts the valley. Vary: warmup_episodes ∈ {0, 1, 3, 5}. Measure: failure rate distribution.
- `conflict_geometry` — A 3-objective sandbox (rescue + resource + escape) produces a different paralysis pattern than 2-objective. Build mini-sandbox; measure failure across κ.
- `outcome_boolean_gate` — Sample failure encodings with probability p (boolean, not magnitude-gated) while always encoding rescues. Vary: p ∈ {0.1, 0.25, 0.5, 1.0}. Measure: divergence@ep9 at κ=1.0. (Follow-up to signed_threshold lockout.)
- `outcome_importance_modulation` — Instead of thresholding *whether* to encode, scale per-outcome importance: rescue × m_l, failure × m_g. Vary: m_g ∈ {0.3, 0.6, 1.0}, m_l ∈ {0.3, 0.6, 1.0}. Measure: rescue rate trajectory.
- `joint_outcome_intensity_filter` — Encode IF outcome=rescue OR (outcome=failure AND e_max > τ). Cleanest decoupling of "whether" from "how loud". Vary: τ ∈ {0.0, 0.3, 0.5, 0.7}. Measure: ep9 rescue rate.

## Done (most recent at top)

- 2026-06-04: impact_decomp_beta_sweep — PARTIAL; impact gap monotone 0.016→0.077 (Prediction A confirmed), threshold β_loyalty* ≈ 0.35–0.40 confirmed; rescue penalty not detectable at N=40 (range ±7.5 pts inside 2-SE ≈ 14 pts), prior 7.5 pt over-steering penalty likely noise.
- 2026-05-29: capped_floor_impact_decomp — Prediction B CONFIRMED (downstream cancellation); at β_loyalty=0.50 seed_refresh_capped inflates seed impact by 0.113 vs seed_only_floor but rescues 7.5 pts LESS (72.5% vs 80.0%) — amplified guilt recall over-steers without improving timing.
- 2026-05-21: capped_loyalty_n200_endpoints — HELD; N=200 replication of yesterday's β_loyalty ∈ {0.05, 0.50} endpoints collapses the substitutability gap from {17.5, 17.5} pts to {0.0, 3.5} pts — a 10× reduction, both inside the ≈6.5 pt 2-SE band. β=0.05 delivers an exact point-estimate match (capped and floor both −5.5 pts vs OFF). MemoryImpact-amplification reading falsified at the headline cell. The OFF baseline itself drifted 17 pts (62.5% → 79.5%) between N=40 runs at the symmetric cell — the apparent "consistent direction" endpoint blowouts were a single common-cause OFF-baseline noise event. 2026-05-20 PARTIAL upgrades to HELD: source-vs-gate isomorphism IS channel-symmetric across both decay axes. Architectural compression (per-memory floor field + one-line max guardrail at the source replaces the injection-time tag dispatch) holds with no measurable residual along guilt OR loyalty axes at the headline cell.
- 2026-05-20: capped_floor_loyalty_sweep — PARTIAL; flipping the swept axis (β_guilt=0.05 fixed, β_loyalty ∈ {0.05, 0.15, 0.30, 0.50}) preserves yesterday's source-vs-gate equivalence at mid-asymmetry (|Δcapped − Δfloor| = 5.0 pts at β=0.15 and β=0.30, matching yesterday's guilt-axis vector exactly) but blows it out at both endpoints (17.5 pts at β=0.05 and β=0.50). The endpoint divergence is directionally consistent: capped > floor in both cells (capped Δep0=+27.5/+17.5 vs floor +10.0/+0.0). Mean substitutability gap doubles (5.6 → 11.3 pts). Candidate mechanism: capped lifts emotion_magnitude on the source, amplifying the seed's exp(γ·|emotion|) recall-impact term BEFORE the gate runs — an asymmetry the floor-at-gate can't replicate. The yesterday-claimed architectural compression survives at the asymmetric mid-cells but is not a clean isomorphism on the loyalty axis.
- 2026-05-19: seed_refresh_capped — HELD; replacing yesterday's unconditional overwrite with the one-sided guardrail m.emotion[k] := max(stored[k], SEED_FLOOR[k]) per dim reproduces the seed-only-floor Δep0 vector at every β_guilt cell (max |Δcapped−Δfloor|=7.5 pts at β=0.05, 5.0 elsewhere — all inside the 2-SE band of ≈22 pts for a Δ-of-Δ at N=40). The β=0.05/0.15 over-restoration pathology that broke seed_refresh is eliminated (capped delivers +5 and +2.5 where refresh delivered −10 and −17.5). Architectural compression: the entire TAG_FLOORS_DEFAULT dispatch table and inject_recalled_emotion_tag_aware code path collapse into a single per-memory `floor` field set at encoding time plus a one-line max guardrail at the source — no per-tag dispatch, no injection-time logic. Second compression step in the floor-mechanism chain (2026-05-12 compressed the TABLE; today compressed the APPLICATION POINT).
- 2026-05-18: seed_refresh — PARTIAL; bypassing the tag-floor table by refreshing the seed memory's stored.emotion to encoding magnitudes every step reproduces the seed-only-floor lift at β_guilt=0.50 (Δrefresh=+40.0 pts vs Δfloor=+37.5 pts, |Δ−Δ|=2.5 pts inside 2-SE) but HURTS ep0 by 10.0 pts at β_guilt=0.05 and 17.5 pts at β_guilt=0.15 where the floor helps. The floor's max(stored, floor) guardrail is mechanism-essential off-headline. Floor TABLE is compressible to one entry; floor OPERATOR is not eliminable.
- 2026-05-13: seed_only_floor_b30_n200 — HELD; replicating the β_guilt=0.30 cell at N=200 collapses the prior |Δfull − Δseed| ep0 gap from 15.0 pts to 1.0 pt (2-SE ≈ 9.2 pts). Δfull=−1.0, Δseed=+0.0 — statistically indistinguishable. The 2026-05-12 "HELD with caveat" upgrades to HELD across all four β_guilt cells; per-class outcome floors confirmed inert at ep0 across the swept asymmetry range. Residual 4.8-pt ep5–7 full-vs-seed gap is within noise but flagged as a long-chain follow-up.
- 2026-05-12: seed_only_floor — HELD with caveat; pruning the tag-aware injection floor table to ONLY 'seed' reproduces the full-floor Δep0 at 3/4 cells. At β_guilt=0.50 BOTH modes deliver identical +10 pts ep0 uplift and +9 pts ep5–9 uplift — per-class outcome floors are inert at the regime-breaking cell. Lone disagreement at β_guilt=0.30 (Δfull=0 vs Δseed=−15) is a 2-SE gap pending N=200 replication. Mechanism: the aged seeded prior is what the floor restores; outcome memories are too young (or too laundered) for the failure/rescue/timeout floors to matter.
- 2026-05-11: tag_aware_injection — FAILED at headline cell; Δep0 at β_guilt=0.50 = −2.0 pts (66% INJ-ON vs 68% INJ-OFF). Tag-keyed injection floors do NOT close the residual β_guilt=0.50 ep0 collapse. Unexpected secondary: +20.0 pts at the symmetric β_guilt=0.05 cell — the floor fixes natural aging of the seeded prior, not laundered failure memories. Δep0 vector monotonically decreasing in β_guilt: {+20.0, +8.0, +14.0, −2.0} pts. The injection pathway is not the second laundering mechanism.
- 2026-05-02: **paralysis_softmax_fix — TWO SUB-MECHANISMS REVEALED.** At κ=0.5 (valley shoulder), low T (T=0.05) rescues from 33% to 43%. At κ=0.25 (valley peak), no T value rescues — 91-100% TIMEOUT. The Paralysis Valley is not single-mechanism: DITHER at the shoulder, LOCK-IN at the peak. The peak failure is "agent commits to non-progressive argmin and never breaks out," not "dither between options."
- 2026-05-02: **jitter_sigma_long — LONG-HORIZON STABILIZATION CONFIRMED.** σ=0.40 at κ=2.0 yields 84.4% sustained rescue rate at ep15-19, stable to within 1 pt of ep5-9 (84.8%). The Encoding Diversity Effect is monotonically increasing in σ up to σ=0.40 — no observed saturation. At long horizons it's 3.30× baseline (84.4% vs 25.6%).
- 2026-05-02: **jitter_universality — THE κ-SCALING OF THE ENCODING DIVERSITY EFFECT.** Jitter rescues the committed regime with strength scaling in κ: +2.4 pts at κ=0.5, +16.4 at κ=1.0, +43.6 at κ=2.0. At κ=2.0 the regime is COMPLETELY STABILIZED (76% rescue rate held flat across episodes 1-9). Jitter does NOT help at the Paralysis Valley (κ=0.25-0.5: 0% gain). The two named failure modes have DISTINCT mechanisms.
- 2026-05-02: **personality_emergence — THE ENCODING DIVERSITY EFFECT (FIRST POSITIVE FINDING).** Per-agent encoding jitter (σ=0.15) yields a 2.55× sustained rescue rate (15.4% → 39.2% avg ep5-9) at κ=1.0. Bounded memory alone is null; jitter alone is the lever; bounded+jitter adds nothing. Behavioral TYPES still don't emerge (div@5-9 ~+3-6 pts) but population CAPACITY is restored. The bottleneck is encoder homogeneity, not memory size.
- 2026-05-02: memory_capacity — NULL on its own. Bounded store with least-impact eviction (cap ∈ {2,3,5,10,∞}) does not break the Homogenization Collapse; sustained rescue stays in [15%, 26%]. Memory size is not the bottleneck.
- 2026-05-09: tag_aware_recall — held; tag-keyed guilt-recall gate ≈doubles ep5–9 mean rescue at every β_guilt cell ({+22.4, +15.2, +18.0, +18.0} pts uplift) and eliminates the β_guilt=0.30 regime-break (ep0 rescue 48% → 76%). Confirms valence laundering at the recall gate as the divergence-erosion mechanism. Residual β_guilt=0.50 ep0 collapse (58% vs 78% symmetric baseline) flags a second mechanism on the injection pathway.
- 2026-05-08: laundering_kappa_invariance — held; failure-memory laundering rate at end-of-ep4 = {0%, 80.7%, 76.5%, 81.1%} across β_guilt ∈ {0.05, 0.15, 0.30, 0.50} at κ=0.5, max cross-κ Δ = 3.2 pts vs the κ=1.0 audit. Mechanism pinned to decay arithmetic; macro divergence unmeasurable at κ=0.5 because ep1 collapses to 0% rescue universally.
- 2026-05-07: memory_population_audit — confirmed per-class memory weight as the divergence-erosion mediator; sharper mechanism is "valence laundering": 78% of failure-tagged memories at β_guilt=0.15 have stored loyalty > stored guilt at recall time (vs 0% at symmetric β=0.05); per-agent guilt-channel total drops 0.95 → 0.48.
- 2026-05-06: decay_asymmetry_reversed — falsifies the 2026-05-05 cushion-vs-counterweight interpretation; divergence@5–9 falls toward 0 across β_guilt ∈ {0.05, 0.15, 0.30} (+10.6 → +3.6 → −1.5 pts) instead of growing positive, and β_guilt=0.50 breaks the committed regime (ep0 84% → 44%). Symmetric mild decay (β=0.05) confirmed across two independent runs as the only regime preserving experience-driven type formation.
- 2026-05-05: decay_asymmetry — failed in opposite direction; ep5–9 mean rescue flat at 18.8–20.8% (range 2.0 pts) across β_loyalty ∈ {0.05, 0.15, 0.30, 0.50} with β_guilt=0.05, but divergence@5–9 inverts sign monotonically from +13.3 pts (symmetric) to −18.8 pts (extreme asymmetry).
- 2026-05-04: loyalty_importance_floor — NULL: ep5–9 rescue rate flat at 14–17% across rescue_importance ∈ {0.0, 0.1, 0.3, 0.5, 0.7}, range 2.4 pts; all ≥12 pts below 28% OFF baseline. Boomerang is structural, not importance-driven.
- 2026-05-03: signed_threshold_encoding — asymmetric τ_guilt/τ_loyalty gates FAIL in opposite direction; high-τ_guilt cells collapse to 0% ep9 rescue (prior-dilution lockout), best divergence +5.1 pts (inferior to symmetric baseline).
- 2026-05-02: valenced_encoding — bidirectional outcome encoding makes collapse WORSE, not better. ep9 rescue rate 28% (off) vs 15% (on); div@5–9 = −10 pts (on). Naming: The Loyalty Boomerang.
- 2026-05-01: selective_encoding — magnitude-gated outcome encoding does NOT prevent the Homogenization Collapse; produces a U-shape (max divergence@ep9 = 11.5 pts at τ=0.3, 0 pts at τ≥0.5).
